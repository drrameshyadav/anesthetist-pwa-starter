import React from 'react';

/**
 * Temporary stub to unblock builds.
 * The original QuickInfo used the legacy patient API (age, weightKg, sex as "male" | "female").
 * We will re-implement it later against the new patient module.
 */
export default function QuickInfo() {
  return null;
}
